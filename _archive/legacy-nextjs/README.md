# Legacy Next.js Archive

Archived on June 29, 2026 during the Astro repository-hygiene pass.

This directory contains the retired Next.js App Router source, React-only components, Next configuration, legacy inductee Markdown/data generators, duplicate derived datasets, and public assets used only by that implementation. It is preserved for historical comparison and must not be treated as build input or current platform guidance.

The active application lives in the repository's `src/pages/`, active `.astro` components, and current `src/data/` files.
