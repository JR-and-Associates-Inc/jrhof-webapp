# Repository Archive

This directory preserves superseded implementation artifacts that may still be useful for migration traceability. Nothing under `_archive/` is part of the active Astro application, TypeScript check, or deployment.

- `legacy-nextjs/` contains the retired Next.js application and its connected generators/data/assets.

Do not fix or import archived code in place. If a historical idea is needed, reimplement it against the active Astro architecture with normal review.
