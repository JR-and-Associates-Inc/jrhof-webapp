# Permanent R2 media origin. Production must set this explicitly only after
# its release candidate has passed the media-origin and gallery UX checks.
# Leave it unset to preserve the tracked 2024 local fallback.
PUBLIC_MEDIA_BASE_URL=https://media.jrhof.org
