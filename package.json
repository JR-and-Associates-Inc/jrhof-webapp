{
  "name": "jrhof-webapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production Astro site for the Joe Rossi Umpires Hall of Fame",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev --config wrangler.jsonc",
    "check": "astro check",
    "test": "node scripts/test-banquet-public-page.mjs",
    "validate": "node scripts/validate-foundation.mjs && node scripts/audit-launch-readiness.mjs",
    "verify": "npm run check && npm run build && npm run validate && npm test",
    "event:media": "node scripts/event-media.mjs",
    "portraits": "node scripts/optimize-inductee-portraits.mjs",
    "deploy": "npm run build && wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "sharp": "^0.35.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.136.1"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
